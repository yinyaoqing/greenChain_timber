# 綠鏈林匯 Week 4 — 整合上線與收尾 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成里程碑 M4 — 公開網址端到端可展示：防冷啟動就緒、AT-1～AT-6 於正式環境驗收並出報告、展示資料齊備、UI 打磨、README 與 Demo 腳本完稿，打 `m4-release` tag。

**Architecture:** 部署已於 W2/W3 提前完成（Vercel + Render + CI/CD + 合約上鏈），本週為收尾：以 GitHub Actions cron 取代 UptimeRobot 做 keepalive（免新帳號）；UI 打磨嚴守 R5 範圍（不做 3D 視覺工程）；驗收混合自動化（scripted AT）與使用者瀏覽器操作（記入 acceptance_report）。

**Tech Stack:** 既有全端堆疊；GitHub Actions cron；SQL（展示資料整理）

**對應文件:** 《開發計畫 v1.0》T4.1–T4.8、《規格書 v1.0》§12 驗收、R3/R5

## Global Constraints

- 正式環境：前端 `https://green-chain-timber-wheat.vercel.app`、後端 `https://greenchain-backend-mp5a.onrender.com`、合約 `0x3fc1c4F56F7dc4A0b52Fd9B62dC1AEECdAce44F8`（Amoy）、瀏覽器 `https://amoy.polygonscan.com`
- 免責標示兩處字串不得變動：`本平台為技術展示原型，估算值不構成經查證之碳權`／`示範估算值，非查證碳權`
- R5 砍功能順序約束：UI 打磨僅限本計畫列出的具體項目，不做 3D 視覺工程與曲線動畫
- Render 免費方案 15 分鐘閒置休眠 → keepalive 每 10 分鐘 ping（R3）；repo 為 public，GitHub Actions 分鐘數不受限
- 「延文實驗林場 A 區」為主展示資料（token #1），**不得改名或刪除**
- Commit 格式 `T4.x: <內容>`；前端指令於 `frontend/`、後端於 `backend/`
- 使用者依賴：Task 6 的 AT-1（瀏覽器圈地）需使用者操作；Render chain 環境變數需已設定（正式站上鏈）

---

### Task 1: Keepalive workflow（T4.2，R3）

**Files:**
- Create: `.github/workflows/keepalive.yml`
- Modify: `docs/deploy.md`（注意事項段落更新）

**Interfaces:**
- Consumes: 無
- Produces: 每 10 分鐘 ping 後端 `/healthz` 的排程 job；供 Task 6 驗收「冷啟動已緩解」

- [ ] **Step 1: 建立 keepalive.yml**

```yaml
name: Keepalive

on:
  schedule:
    # 每 10 分鐘 ping 一次，防 Render 免費方案 15 分鐘閒置休眠（R3）
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend healthz
        run: |
          code=$(curl -s -o /dev/null -w "%{http_code}" --max-time 90 \
            https://greenchain-backend-mp5a.onrender.com/healthz)
          echo "healthz -> HTTP $code"
          test "$code" = "200"
```

- [ ] **Step 2: 更新 deploy.md 注意事項**

將 `docs/deploy.md` 中「W4 會設 UptimeRobot 每 10 分鐘 ping `/healthz`」一句改為：

```markdown
- **Render 免費方案冷啟動 ~30–60 s**（規格書 R3）：已由 `.github/workflows/keepalive.yml`
  每 10 分鐘 ping `/healthz` 保持喚醒（GitHub Actions cron，取代 UptimeRobot 免建帳號）；
  cron 偶有延遲屬正常，Demo 前仍建議手動開一次網址預熱
```

- [ ] **Step 3: 驗證與 Commit**

Commit 後於 GitHub Actions 頁面以 **workflow_dispatch 手動觸發一次**，確認 job 綠燈且輸出 `healthz -> HTTP 200`（若後端正在休眠，第一次可能 timeout——重跑一次即可）。

```powershell
git add .github/workflows/keepalive.yml docs/deploy.md
git commit -m "T4.2: keepalive workflow（GH Actions cron 每 10 分鐘 ping healthz）"
git push origin main
```

---

### Task 2: 展示資料整理（T4.4）

**Files:**
- Create: `backend/scripts/rename_demo_plots.sql`（記錄用；實際以 psql/asyncpg 執行）

**Interfaces:**
- Consumes: 正式 DB（`backend/.env` DATABASE_URL）
- Produces: 儀表板 4 筆林區名稱具展示性；名稱僅存 DB（鏈上只有 geoHash），改名不影響 AT-6

- [ ] **Step 1: 清點現有林區**

於 `backend/` 執行（列出全部名稱與狀態）：

```powershell
uv run python -c "
import asyncio, asyncpg
async def main():
    url = [l.split('=',1)[1].strip() for l in open('.env', encoding='utf-8') if l.startswith('DATABASE_URL=')][0]
    conn = await asyncpg.connect(url)
    for r in await conn.fetch('select id, name, species, status from forest_plots order by created_at'):
        print(str(r['id'])[:8], r['species'], r['status'], r['name'])
    await conn.close()
asyncio.run(main())
"
```

- [ ] **Step 2: 改名（保留「延文實驗林場 A 區」）**

建立 `backend/scripts/rename_demo_plots.sql` 記錄改名意圖：

```sql
-- 展示資料名稱整理（T4.4）。名稱僅存 DB，鏈上只有 geoHash，改名不影響 AT-6。
-- 「延文實驗林場 A 區」(token #1) 為主展示資料，不改。
update forest_plots set name = '太平山示範林 B 區' where name = 'M3 上鏈驗證區';
update forest_plots set name = '棲蘭示範林 C 區'   where name = 'AT-5 重試演練區';
-- 使用者 M2 測試建立的林區（Step 1 清點後將實際名稱代入下行執行）：
-- update forest_plots set name = '頭城示範林 D 區' where name = '<實際測試名稱>';
```

以 asyncpg 執行上述 update（含 Step 1 查到的使用者測試林區實際名稱），再重跑 Step 1 確認 4 筆名稱皆具展示性、樹種涵蓋三種。

- [ ] **Step 3: Commit**

```powershell
git add backend/scripts/rename_demo_plots.sql
git commit -m "T4.4: 展示資料名稱整理（4 筆示範林區、三樹種齊備）"
```

---

### Task 3: UI 打磨（T4.5，嚴守清單）

**Files:**
- Create: `frontend/lib/format.ts`
- Create: `frontend/app/not-found.tsx`
- Modify: `frontend/app/dashboard/page.tsx`（載入 skeleton + 數字格式化）
- Modify: `frontend/components/PlotDetailView.tsx`（數字格式化）
- Modify: `frontend/app/login/page.tsx`（已登入自動導向）

**Interfaces:**
- Consumes: 既有元件
- Produces: `format.ts`：`formatHa(v: number): string`、`formatCo2e(v: number): string`

- [ ] **Step 1: lib/format.ts**

```ts
/** 數字顯示格式化（千分位；面積 2 位小數、碳匯 2 位小數） */
export function formatHa(v: number): string {
  return `${v.toLocaleString("zh-TW", { minimumFractionDigits: 2, maximumFractionDigits: 2 })} ha`;
}

export function formatCo2e(v: number): string {
  return `${v.toLocaleString("zh-TW", { maximumFractionDigits: 2 })} 噸 CO₂e/年`;
}
```

- [ ] **Step 2: 儀表板 skeleton + 格式化**

`frontend/app/dashboard/page.tsx`：import `formatHa, formatCo2e`；`if (plots === null)` 的「載入中…」改為 skeleton：

```tsx
  if (plots === null) {
    return (
      <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
        {Array.from({ length: 6 }).map((_, i) => (
          <div
            key={i}
            className="h-44 animate-pulse rounded-xl border border-stone-200 bg-stone-100"
          />
        ))}
      </div>
    );
  }
```

卡片中 `{p.area_ha.toFixed(4)} ha` → `{formatHa(p.area_ha)}`；`` {`${p.co2e_current.toFixed(2)} 噸 CO₂e/年`} `` → `{formatCo2e(p.co2e_current)}`（null 分支「—」不變）。

- [ ] **Step 3: 詳情頁格式化**

`frontend/components/PlotDetailView.tsx`：import `formatHa`；側欄「面積」`{plot.area_ha.toFixed(4)} ha` → `{formatHa(plot.area_ha)}`（geo_hash、曲線等不動）。

- [ ] **Step 4: 登入頁已登入導向**

`frontend/app/login/page.tsx`：加入

```tsx
import { useEffect } from "react";
import { useSession } from "@/hooks/useSession";
```

元件內：

```tsx
  const { session, loading: sessionLoading } = useSession();
  useEffect(() => {
    if (!sessionLoading && session) router.replace("/draw");
  }, [sessionLoading, session, router]);
```

- [ ] **Step 5: not-found.tsx**

```tsx
import Link from "next/link";

export default function NotFound() {
  return (
    <div className="mx-auto max-w-md px-4 py-24 text-center">
      <p className="text-6xl">🌲</p>
      <h1 className="mt-4 text-2xl font-bold text-emerald-900">找不到這片森林</h1>
      <p className="mt-2 text-stone-600">頁面不存在或已被移除。</p>
      <Link
        href="/"
        className="mt-6 inline-block rounded-lg bg-emerald-700 px-6 py-2.5 text-white hover:bg-emerald-800"
      >
        回首頁
      </Link>
    </div>
  );
}
```

- [ ] **Step 6: 驗證與 Commit**

Run（`frontend/`）: `npm run lint` → 0 errors；`npm run build` → 成功。

```powershell
git add frontend/
git commit -m "T4.5: UI 打磨（skeleton/數字格式化/登入導向/404 頁）"
```

---

### Task 4: README 完整化（T4.7）

**Files:**
- Modify: `README.md`（整檔改寫）

- [ ] **Step 1: 改寫 README.md（以下內容為完整定稿）**

```markdown
# 🌲 綠鏈林匯 GreenChain Timber

數位 MRV（Measurement, Reporting, Verification）碳權透明化平台 MVP——
讓小型林主的森林碳匯**可量測、可驗證、不可竄改**。

> 本平台為技術展示原型，估算值不構成經查證之碳權。

**Live Demo:** https://green-chain-timber-wheat.vercel.app
**鏈上合約:** [`0x3fc1...44F8`](https://amoy.polygonscan.com/address/0x3fc1c4F56F7dc4A0b52Fd9B62dC1AEECdAce44F8)（Polygon Amoy 測試網）

## 核心流程

地圖圈選林地 → PostGIS 空間防重疊檢查 → 農業部公式自動估算固碳量（當年 + 未來 5 年）
→ ERC-721 NFT 上鏈存證（幾何 SHA-256 指紋 + 碳噸數）→ 企業儀表板展示與鏈上驗證

## 系統架構

```
┌─────────────────────────────────────────────────────┐
│  Next.js 16 + Tailwind + Mapbox GL + Recharts        │
│  Vercel · green-chain-timber-wheat.vercel.app        │
└──────────────┬──────────────────────────────────────┘
               │ HTTPS / JSON (Supabase JWT, ES256/JWKS)
┌──────────────▼──────────────────────────────────────┐
│  FastAPI (Python 3.12) · Render                      │
│  ├─ /api/forest         圈地→防重疊→估算→入庫→觸發上鏈 │
│  ├─ carbon_calc         農業部公式估算（MOA-2024-v1） │
│  ├─ geo_service         幾何驗證/正規化/SHA-256       │
│  └─ chain_service       Web3.py Mint + 重試 + 回寫    │
└─────┬────────────────────────────┬──────────────────┘
      │ asyncpg                     │ JSON-RPC
┌─────▼──────────────┐   ┌─────────▼───────────────────┐
│ Supabase            │   │ Polygon Amoy (chainId 80002) │
│ PostgreSQL + PostGIS│   │ GreenAssetNFT (ERC-721)      │
│ + Auth (ES256)      │   │ geoHash 唯一性防重複申報      │
└─────────────────────┘   └──────────────────────────────┘
```

## Monorepo 結構

| 目錄 | 內容 | 工具鏈 |
|---|---|---|
| `backend/` | FastAPI + asyncpg + web3.py | uv（Python 3.12） |
| `frontend/` | Next.js App Router | npm（Node 20） |
| `contracts/` | GreenAssetNFT + Hardhat 測試/部署 | npm（Hardhat 2.x） |
| `docs/` | 規格書、開發計畫、devlog、部署指南、驗收報告 | — |

## 本機啟動

前置：Node 20、Python 3.12、[uv](https://docs.astral.sh/uv/)、各服務帳號（Supabase / Mapbox）。

### 後端

```bash
cd backend
cp .env.example .env        # 填 DATABASE_URL / SUPABASE_URL（chain 變數可留空）
uv sync
uv run python scripts/apply_schema.py   # 首次：建三表 + PostGIS 索引
uv run uvicorn app.main:app --port 8000
```

### 前端

```bash
cd frontend
cp .env.local.example .env.local        # 填四個 NEXT_PUBLIC_ 變數
npm install
npm run dev                             # http://localhost:3000
```

### 合約（選配，本機不部署也能跑核心流程）

```bash
cd contracts
npm install
npx hardhat test                        # 8 個合約測試
npm run deploy:amoy                     # 需 .env 私鑰 + Amoy 測試幣
```

## 測試

| 範圍 | 指令 | 說明 |
|---|---|---|
| 後端單元 | `cd backend && uv run pytest` | 79 tests；整合測試需 `TEST_DATABASE_URL` |
| 合約 | `cd contracts && npx hardhat test` | mint/權限/geoHash 唯一性/tokenURI |
| 前端 | `cd frontend && npm run lint && npm run build` | 型別檢查 + prerender |
| AT-6 雜湊驗證 | `cd backend && uv run python scripts/verify_hash.py` | DB 重算 vs 鏈上 geoHash |

CI（GitHub Actions）於每次 push 跑三個 job：backend（ruff+pytest）、frontend（eslint+build）、contracts（hardhat test）。

## 部署

見 [docs/deploy.md](docs/deploy.md)——Vercel（前端）+ Render Blueprint（後端）+ keepalive workflow。
環境變數清單見各目錄 `.env.example`。

## 里程碑

| Tag | 內容 |
|---|---|
| `m1-backend` | 後端 + 空間資料庫（防重疊/估算/JWT/JWKS） |
| `m2-frontend` | 3D 圈地 + 儀表板（手動 E2E 6/6） |
| `m3-chain` | 合約部署 + 上鏈全通（AT-5/AT-6 過） |
| `m4-release` | 正式環境驗收 + 展示就緒 |

## 已知限制

- 估算係數為文獻常見值 placeholder（`MOA-2024-v1`），正式係數待農業部文獻查證
- 單一角色、無 RBAC/RLS；碳權交易、遙測判釋、主網部署均為 out of scope（見規格書 §1.4）
- Amoy 為測試網，NFT 不具真實資產價值
```

- [ ] **Step 2: Commit**

```powershell
git add README.md
git commit -m "T4.7: README 完整化（架構/啟動/測試/里程碑/限制）"
```

---

### Task 5: Demo 腳本（T4.6）

**Files:**
- Create: `docs/demo_script.md`

- [ ] **Step 1: 建立 demo_script.md（完整定稿）**

```markdown
# Demo 腳本

事前準備（Demo 前 10 分鐘）：
1. 開 https://green-chain-timber-wheat.vercel.app 預熱（Render 冷啟動保險）
2. 登入測試帳號；確認儀表板 4 筆林區、「延文實驗林場 A 區」為 on_chain
3. 備妥分頁：Amoy 合約頁 https://amoy.polygonscan.com/address/0x3fc1c4F56F7dc4A0b52Fd9B62dC1AEECdAce44F8
4. 本機備妥終端（8 分鐘版的雜湊驗證用）：`cd backend`

---

## 3 分鐘版（G1 快速走查：圈地 → 估算 → 上鏈 → 驗證）

| 時間 | 動作 | 講點 |
|---|---|---|
| 0:00–0:20 | Landing 頁 | 「台灣森林碳匯三大信任落差：量測不透明、重複申報、憑證可竄改——我們用一條流程解掉」 |
| 0:20–1:10 | 圈地申報：3D 地圖圈一塊 5–20 ha 林地（工具自動轉俯視），即時面積顯示 | 「衛星 + 3D 地形，林主滑鼠圈出自家林地；面積即時計算，超出 0.1–1000 ha 直接擋」 |
| 1:10–1:50 | 填表（台灣杉/15 年/1500 株）送出 → 成功卡 + 6 年固碳曲線 | 「農業部公式自動估算，當年 + 未來 5 年；每筆估算綁公式版本可追溯」 |
| 1:50–2:30 | 儀表板 → 點入該林區詳情：飛行定位 + 綠色邊界 + 「上鏈處理中」動畫 → 轉 on_chain | 「送出當下已開始鑄造 NFT——不阻塞使用者，失敗自動重試」 |
| 2:30–3:00 | 點 Tx Hash → Amoy 瀏覽器交易頁 | 「憑證不在我們手上，在公開區塊鏈上——任何人可獨立驗證」 |

## 8 分鐘版（加入防重疊、鏈上防偽、雜湊比對三個亮點）

前 3 分鐘同上，接著：

| 時間 | 動作 | 講點 |
|---|---|---|
| 3:00–4:30 | **重複申報防線一**：再圈一塊與剛才重疊的林地送出 → 地圖紅色高亮衝突區 + 衝突面積 | 「double counting 是碳權市場的道德風險核心——PostGIS 空間交集檢查，重疊 0.001 公頃就擋」 |
| 4:30–5:30 | **防線二**：Amoy 合約頁 → Read Contract → `geoHashUsed` 貼入該林區 geoHash（詳情頁側欄可複製）→ 回傳 true | 「就算繞過我們的後端，鏈上合約本身拒絕同一幾何指紋二次鑄造」 |
| 5:30–7:00 | **可驗證性**：終端跑 `uv run python scripts/verify_hash.py` → 逐筆 MATCH | 「資料庫的邊界座標重算 SHA-256，與鏈上指紋完全一致——資料庫被竄改一個小數點都會 MISMATCH」 |
| 7:00–8:00 | 合約頁 `getPlotData(1)` 展示鏈上存的 geoHash/碳公斤數/樹種代碼；收尾 | 「鏈上只存指紋與噸數（省 gas、不洩地籍），原始資料留庫、隨時可比對——這就是 MRV 的 V」 |

## 風險備案

- Render 冷啟動：keepalive 已設；若仍遇到，開場先點一次儀表板讓它醒
- Amoy 測試網抖動：mint 卡住時說明重試機制（詳情頁停在處理中動畫即是活 demo），
  後台 `POST /api/admin/retry-pending` 可手動補鑄
- Mapbox 載入慢：先開好 /draw 分頁備用
```

- [ ] **Step 2: 演練與 Commit**

依 3 分鐘版實際走一次計時（controller/使用者），超時處記入 devlog。

```powershell
git add docs/demo_script.md
git commit -m "T4.6: Demo 腳本（3 分鐘 + 8 分鐘版 + 風險備案）"
```

---

### Task 6: 正式環境 E2E 驗收 AT-1～AT-6 + 驗收報告（T4.3，§12）

**前置：** Render 已設定 4 個 chain 環境變數（使用者）；keepalive 已生效。

**Files:**
- Create: `docs/acceptance_report.md`

**驗收方式分工：** AT-1（瀏覽器圈地全流程）由**使用者**於正式站操作；AT-2/3/4 以 script 直打正式 API；AT-5 引用 M3 本機完整演練紀錄（正式環境不重演壞 RPC——避免動 Render 設定）；AT-6 以 verify_hash.py 對正式 DB + 鏈上執行。

- [ ] **Step 1: scripted 驗收（AT-2/3/4 直打正式 API）**

於 `backend/` 以 Python 對 `https://greenchain-backend-mp5a.onrender.com` 執行（取得 JWT 同既有流程）：
1. AT-2：提交與「延文實驗林場 A 區」重疊之多邊形 → 預期 409 + conflicts（含 overlap_ha 與 GeoJSON）；查 DB 確認無新增
2. AT-3：自相交蝴蝶結多邊形 → 422 `self_intersection`
3. AT-4：avg_age=0 與 density=50 直打 API → 422（欄位驗證，繞過前端）

- [ ] **Step 2: AT-6 於正式資料執行**

`uv run python scripts/verify_hash.py` → 全數 MATCH（正式 DB 與 Amoy 鏈上）。

- [ ] **Step 3: AT-1 使用者操作（正式站）**

使用者於 https://green-chain-timber-wheat.vercel.app 登入 → 圈 5 ha 林地 → 台灣杉/15/1500 → 送出 → 確認：201 成功卡 + 6 年曲線；≤ 2 分鐘詳情頁轉 on_chain；Tx Hash 點擊可於 Amoy 瀏覽器查得。

- [ ] **Step 4: 撰寫 docs/acceptance_report.md**

表格逐案記錄：案例編號、執行方式（scripted/手動）、執行時間、結果（PASS/FAIL）、證據（HTTP 狀態/tx hash/截圖說明）、備註（AT-5 引用 M3 演練：3 次重試 → chain_pending → 恢復 → 5 秒補鑄 token #4）。六案例全 PASS 方可進 Task 7。

- [ ] **Step 5: Commit**

```powershell
git add docs/acceptance_report.md
git commit -m "T4.3: 正式環境驗收報告（AT-1~AT-6 全 PASS）"
```

---

### Task 7: M4 收尾（devlog + tag）

**Files:**
- Modify: `docs/devlog.md`

- [ ] **Step 1: devlog 記錄 M4 達成**

追加：驗收六案例結果摘要、keepalive 生效確認、四週里程碑回顧（M1–M4 日期）、遺留清單（正式係數查證、W2/W3 deferred minors、Mapbox 用量監控）。

- [ ] **Step 2: push + tag**

```powershell
git add docs/devlog.md
git commit -m "T4.8: devlog 記錄 M4 達成（四週里程碑全數完成）"
git push origin main
git tag m4-release
git push origin m4-release
```

---

## 已知風險與後續

- Task 6 依賴使用者兩個動作：Render chain 環境變數（若尚未設定，正式站 mint 會停留 chain_pending——不影響 AT-2/3/4/6 驗收，AT-1 的上鏈段會卡）與 AT-1 瀏覽器操作
- GitHub Actions cron 有 3–15 分鐘不等的排程延遲，keepalive 實效為「大幅減少」而非「完全消除」冷啟動；Demo 前手動預熱仍是標準動作
- 展示錢包餘額 0.04 POL 約可再 mint 15–20 筆；Demo 演練頻繁時再領一次 faucet

## Self-Review 紀錄

- 開發計畫覆蓋：T4.1（已於 W2/W3 提前完成——deploy.md 為證）、T4.2（Task 1，GH Actions 替代 UptimeRobot 記錄於 deploy.md）、T4.3（Task 6）、T4.4（Task 2）、T4.5（Task 3，嚴守 R5）、T4.6（Task 5）、T4.7（Task 4）、T4.8（Task 7 + 本計畫緩衝即各任務彈性）
- Placeholder 掃描：Task 2 SQL 有一行需 Step 1 清點後代入實際名稱——屬執行期資料查詢，非計畫缺口，已於註解標明流程
- 型別一致性：`formatHa`/`formatCo2e` 簽名於 Tasks 3 各步一致；URL/合約地址與 Global Constraints 一致
